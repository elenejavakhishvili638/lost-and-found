export type inputProps = {
    title: string
    type: string
    className: string
    name: string
    value: string
}

export type textareaProps = {
    title: string
    rows: number
    cols: number
    className: string
    name: string
    value: string
}